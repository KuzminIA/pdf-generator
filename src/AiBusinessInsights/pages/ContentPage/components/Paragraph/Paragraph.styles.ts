import { StyleSheet } from '@react-pdf/renderer'

export const styles = StyleSheet.create({
  paragraph: {
    fontSize: '20px',
    fontFamily: 'Roboto',
    marginTop: '20px',
    lineHeight: 1.4,
  }
});
